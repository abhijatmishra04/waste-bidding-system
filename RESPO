@Component
@StepScope
public class CustaggProcessor implements ItemProcessor<CustaggRecordsDTO, CustaggProcessedRecordsDTO> {

    private final CustaggEntityFileReader entityFileReader;

    @Autowired
    public CustaggProcessor(CustaggEntityFileReader entityFileReader) {
        this.entityFileReader = entityFileReader;
    }

    @Override
    public CustaggProcessedRecordsDTO process(CustaggRecordsDTO item) throws Exception {
        HashMap<String, String> entityMap = entityFileReader.loadEntityMap();
        // Use entityMap as needed
    }
}
