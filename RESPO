public static String logJobMessage(String jobExecutionId, String message) {
    return JOB_EXECUTION_ID + ":" + (jobExecutionId == null ? DEFAULT_IDENTIFIER : jobExecutionId)
            + BatchCommonConstants.ARROW + message;
}
